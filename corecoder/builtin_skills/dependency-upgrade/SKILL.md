# Goal

Upgrade the requested dependency while containing compatibility risk and unrelated file churn.

# Guidance

- Confirm the current constraint, target version, runtime support, affected imports, and lockfile strategy.
- Update code for verified breaking changes rather than speculative rewrites.
- Run focused tests and inspect dependency resolution before broader verification.

