# Goal

Define a predictable CLI contract for humans, scripts, and automation.

# Guidance

- Specify command hierarchy, arguments, defaults, stdin/stdout/stderr, exit codes, and destructive-action safeguards.
- Follow existing project conventions and make non-interactive use deterministic.
- Include representative success and failure examples without prematurely implementing them.

