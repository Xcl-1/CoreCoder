# CoreCoder 五大方向优化计划（简版）

> **目标项目**: CoreCoder v0.4.0 | **原则**: 极简、向后兼容、渐进式、文件级存储

---

## 总体路线

```
第一阶段 (基础框架)
├── Skill: 基类 + 注册中心 + 关键词路由 + 3 个内置 Skill
├── Memory: 数据模型 + 文件 CRUD + MEMORY.md 索引
└── Security: PermissionManager + 权限配置 + Guard 基础版

第二阶段 (深化优化)
├── Context: 精确 Token 计数 + L1 语义截断 + L2 增量摘要
├── Multi-Agent: AgentRole 预设角色 + 增强 AgentTool + 异步执行
└── Security: AuditLogger + 用户确认交互

第三阶段 (高级特性)
├── Skill: 文件自动加载 + 依赖解析 + Skill 组合
├── Memory: 提取器 + 检索器 + 完整闭环
├── Multi-Agent: Orchestrator + 协作模式 + WorktreeManager
├── Context: L2.5 结构化摘要 + 动态阈值
└── Security: SandboxPolicy + ContentReviewer
```

---

## 方向一：Skill 能力体系

**优化内容**：从零构建分层意图路由 + 提示词注入的 Skill 系统。Skill 不直接调用工具，而是通过注入专门的系统提示词片段，告诉 LLM 如何用现有工具完成特定任务。

**核心能力**：
- 意图识别：根据用户输入的正则/关键词匹配，自动路由到合适的 Skill
- 提示词注入：匹配到 Skill 后，将其专属提示词片段注入对话上下文
- 内置 Skill：预置 code-review、test-gen、refactor 三个 Skill
- 用户自定义：支持从 `~/.corecoder/skills/*.skill.md` 文件加载自定义 Skill

**作用**：让 Agent 在面对不同任务时自动切换"角色模式"，无需用户手动指定。用户也可在不修改源码的情况下定制 Agent 行为。

---

## 方向二：自进化记忆沉淀

**优化内容**：设计"提取 → 存储 → 检索 → 注入 → 更新"的完整记忆沉淀闭环，让 Agent 具备跨会话学习能力。

**核心能力**：
- 记忆提取：会话结束时，由 LLM 自动分析对话，提取关键信息（用户偏好、项目约定、反馈等）
- 文件存储：每条记忆一个 Markdown 文件，存入 `~/.corecoder/memory/`，用 MEMORY.md 做索引
- 关键词检索：新会话开始时，用关键词匹配 + Jaccard 相似度检索相关记忆，注入系统提示词
- 去重合并：新记忆与已有记忆去重后写入，避免冗余

**作用**：Agent 不再每次从零开始。用户偏好、项目背景、历史反馈等会被沉淀下来，跨会话持续生效，越用越"懂你"。

---

## 方向三：分层上下文压缩

**优化内容**：在现有三级压缩（L1/L2/L3）基础上深化策略，不改变外部接口，全部改动集中在 `context.py`。

**核心优化**：
- 精确 Token 计数：引入 tiktoken（可选依赖）替代现有的 `len//3` 估算，触发更精准
- L1 工具类型感知截断：不同工具采用不同截断策略（如 grep 保留全部、bash 保留头尾），而非统一的前 3 + 后 3 行
- L2 增量摘要：不再每次把全部历史发给 LLM 重新摘要，只摘要新增部分并合并到已有摘要，成本从 O(n²) 降至 O(n)
- L2.5 结构化分层摘要：按信息重要性分层保留（关键信息完整保留，上下文信息一行摘要或丢弃）
- 动态阈值：根据对话轮次自动调整压缩触发阈值

**作用**：在长对话中更智能地保留关键信息，避免重要上下文被误删，同时降低 LLM 调用成本。

---

## 方向四：多 Agent 协作

**优化内容**：将现有的单一同步子 Agent 扩展为中心化多 Agent 协作架构。

**核心能力**：
- 预设角色：planner（规划）、executor（执行）、reviewer（审查）、researcher（研究），每个角色有专属系统提示词和工具集
- 增强 AgentTool：支持异步执行、角色指定、worktree 隔离、工具限定
- Orchestrator 编排器：LLM 驱动的任务分解（产出 DAG），按依赖拓扑序调度并行执行，最后合成结果
- 四种协作模式：Pipeline（串行管道）、Parallel（并行执行）、Debate（多方案辩论）、Verify（执行+审查）
- Git Worktree 隔离：为每个需写文件的 Agent 创建独立 worktree，避免文件冲突

**作用**：让多个 Agent 各司其职、并行工作。复杂任务自动拆解分派，独立子任务并行推进，高风险操作经审查 Agent 把关。

---

## 方向五：权限与安全审查

**优化内容**：将现有的简单 Bash 黑名单（10 个硬编码正则）替换为多层可配置的权限系统。

**核心能力**：
- 五层审查链：静态规则引擎 → 权限策略匹配 → 用户确认交互 → 审计日志记录 → 输出脱敏
- 权限等级：read / write_local / write_global / shell_safe / shell_any / network，精细控制
- 可配置规则：用户级（`~/.corecoder/permissions.json`）和项目级（`.corecoder/permissions.json`），支持 allow / deny / ask 三种动作
- 输出脱敏：自动检测并替换 API Key、Token、密钥等敏感信息
- 审计日志：JSONL 格式，按日期分文件，自动轮转保留 30 天
- 频率限制：支持 per-tool 调用频率限制

**作用**：让用户对 Agent 的操作有精细化的掌控力。知道 Agent 做了什么、阻止危险操作、敏感信息不泄露，安全可审计。

---

## 文件与代码量

| 模块 | 新增文件 | 预估代码量 | 外部依赖 |
|------|---------|-----------|---------|
| Skills | 9 个 | ~360 行 | 无 |
| Memory | 7 个 | ~410 行 | 无 |
| Context | 0（修改 1） | ~230 行 | tiktoken（可选） |
| Multi-Agent | 6 个 | ~490 行 | Git（系统工具） |
| Security | 4 个 | ~450 行 | 无 |
| **总计** | **26 个** | **~1,940 行** | — |

引擎代码量从 ~1,081 行扩展到约 3,000 行，保持一个下午可通读的范围。
